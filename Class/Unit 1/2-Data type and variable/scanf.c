#include<stdio.h>
int main()
{
    int a,b;
    int n=23;
    printf("enter values-");
   // scanf("%d,%d",&a,&b);   //must give ',' when giving inputs
    scanf("%d%d",&a,&b);
    printf("The values of a=%d b=%d",a,b);
    return 0;
}