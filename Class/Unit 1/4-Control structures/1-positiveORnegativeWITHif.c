#include<stdio.h>
int main()
{
    int no;
    printf("Enter no=");
    scanf("%d",&no);
    if (no>0)
    printf("The no. is positive\n");
    if (no<0)
    printf("The no. is negative\n");

    return 0;
}